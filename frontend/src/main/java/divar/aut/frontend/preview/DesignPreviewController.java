package divar.aut.frontend.preview;

/**
 * Controller for the design-system component gallery (DesignPreview.fxml).
 * Intentionally empty: the preview is purely presentational and interactive
 * feedback comes from CSS pseudo-classes (:hover / :pressed).
 */
public class DesignPreviewController {
}
