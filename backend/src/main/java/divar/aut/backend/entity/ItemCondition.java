package divar.aut.backend.entity;

/**
 * A simple category-specific attribute every ad has: whether the item being
 * sold is brand new or has been used before.
 */
public enum ItemCondition {
    NEW,
    USED
}
